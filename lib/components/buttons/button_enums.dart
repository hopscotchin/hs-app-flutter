enum ButtonVariant { primary, secondary, tertiary, link, linksmall }

enum ButtonStyleType { defaultType, hover, inactive }

enum ButtonSize { small, medium, large }

enum ButtonState { enabled, disabled, loading }
