import CTNotificationContent

/// `CTNotificationViewController` implements the full `UNNotificationContentExtension`
/// protocol and renders CleverTap's push template UI (carousel, rating, …); no
/// overrides are required here.
class NotificationViewController: CTNotificationViewController {
}
