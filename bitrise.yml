format_version: '26'
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git
project_type: flutter

workflows:
  _setup:
    steps:
      - activate-ssh-key@4:
          run_if: '{{getenv "SSH_RSA_PRIVATE_KEY" | ne ""}}'
      - git-clone@8: {}
      - file-downloader@1:
          title: Download .env from Bitrise Generic File Storage
          inputs:
            - source: "$BITRISEIO_ENV_FILE_URL"
            - destination: "$BITRISE_SOURCE_DIR/.env"
      - flutter-installer@1:
          inputs:
            - version: "$FLUTTER_VERSION"

  _test:
    before_run:
      - _setup
    steps:
      - flutter-analyze@0:
          inputs:
            - project_location: "$BITRISE_FLUTTER_PROJECT_LOCATION"
      - flutter-test@1:
          inputs:
            - project_location: "$BITRISE_FLUTTER_PROJECT_LOCATION"

  _deploy_android_debug:
    steps:
      - set-java-version@1:
          inputs:
            - set_java_version: 21
      - flutter-build@0:
          title: Build debug APK (universal)
          inputs:
            - project_location: "$BITRISE_FLUTTER_PROJECT_LOCATION"
            - platform: android
            - android_output_type: apk
            - android_additional_params: "--debug"
      - firebase-app-distribution@0:
          title: Firebase Deployment - Debug APK
          inputs:
            - app_path: "$BITRISE_APK_PATH"
            - app: "$FIREBASE_ANDROID_APP_ID"
            - groups: "$TEST_GROUPS"
            - release_notes: "$BITRISE_GIT_MESSAGE"
            - firebase_token: "$FIREBASE_TOKEN"
      # ponytail: AAB distribution disabled — Firebase App Distribution needs
      # Play Store integration linked for the hs-app-flutter project. Re-enable
      # once Firebase Console → Integrations → Google Play is set up.
      # - flutter-build@0:
      #     title: Build debug AAB
      #     inputs:
      #       - project_location: "$BITRISE_FLUTTER_PROJECT_LOCATION"
      #       - platform: android
      #       - android_output_type: appbundle
      #       - android_additional_params: "--debug"
      # - firebase-app-distribution@0:
      #     title: Firebase Deployment - Debug AAB
      #     inputs:
      #       - app_path: "$BITRISE_AAB_PATH"
      #       - app: "$FIREBASE_ANDROID_APP_ID"
      #       - groups: "$TEST_GROUPS"
      #       - release_notes: "$BITRISE_GIT_MESSAGE"
      #       - firebase_token: "$FIREBASE_TOKEN"
      - deploy-to-bitrise-io@2: {}

  _deploy_ios_debug:
    steps:
      - script:
          title: flutter pub get + iOS config prep
          inputs:
            - content: |-
                #!/usr/bin/env bash
                set -ex
                flutter pub get
                dart pub global activate flutterfire_cli
                envman add --key PATH --value "$HOME/.pub-cache/bin:$PATH"
                flutter build ios --config-only --debug
      - manage-ios-code-signing@2:
          inputs:
            - distribution_method: development
            - apple_service_connection: api-key
            - project_path: "./ios/Runner.xcworkspace"
            - scheme: Runner
      - xcode-archive@5:
          inputs:
            - project_path: "./ios/Runner.xcworkspace"
            - scheme: Runner
            - configuration: Debug
            - distribution_method: development
            - automatic_code_signing: api-key
      - firebase-app-distribution@0:
          title: Firebase Deployment - Debug IPA
          inputs:
            - app_path: "$BITRISE_IPA_PATH"
            - app: "$FIREBASE_IOS_APP_ID"
            - groups: "$TEST_GROUPS"
            - release_notes: "$BITRISE_GIT_MESSAGE"
            - firebase_token: "$FIREBASE_TOKEN"
      - deploy-to-bitrise-io@2: {}

  bundle_android:
    description: Test + Debug APK/AAB, deployed to Firebase App Distribution.
    before_run:
      - _test
    after_run:
      - _deploy_android_debug

  bundle_iOS:
    description: Test + Debug iOS build (dev-signed), deployed to Firebase App Distribution.
    before_run:
      - _test
    after_run:
      - _deploy_ios_debug

  bundle_flutter:
    description: Test + Android + iOS debug builds, deployed to Firebase App Distribution.
    before_run:
      - _test
    after_run:
      - _deploy_android_debug
      - _deploy_ios_debug

  release_android:
    description: Test + Release APK/AAB, deployed to Firebase App Distribution.
    before_run:
      - _test
    steps:
      - set-java-version@1:
          inputs:
            - set_java_version: 18
      - flutter-build@0:
          title: Build release APK (universal)
          inputs:
            - project_location: "$BITRISE_FLUTTER_PROJECT_LOCATION"
            - platform: android
            - android_output_type: apk
      - firebase-app-distribution@0:
          title: Firebase Deployment - Release APK
          inputs:
            - app_path: "$BITRISE_APK_PATH"
            - app: "$FIREBASE_ANDROID_APP_ID"
            - groups: "$TEST_GROUPS"
            - release_notes: "$BITRISE_GIT_MESSAGE"
            - firebase_token: "$FIREBASE_TOKEN"
      # ponytail: AAB distribution disabled — Firebase App Distribution needs
      # Play Store integration linked for the hs-app-flutter project. Re-enable
      # once Firebase Console → Integrations → Google Play is set up.
      # - flutter-build@0:
      #     title: Build release AAB
      #     inputs:
      #       - project_location: "$BITRISE_FLUTTER_PROJECT_LOCATION"
      #       - platform: android
      #       - android_output_type: appbundle
      # - firebase-app-distribution@0:
      #     title: Firebase Deployment - Release AAB
      #     inputs:
      #       - app_path: "$BITRISE_AAB_PATH"
      #       - app: "$FIREBASE_ANDROID_APP_ID"
      #       - groups: "$TEST_GROUPS"
      #       - release_notes: "$BITRISE_GIT_MESSAGE"
      #       - firebase_token: "$FIREBASE_TOKEN"
      - deploy-to-bitrise-io@2: {}

  release_iOS:
    description: Test + Release iOS archive (ad-hoc signed), deployed to Firebase App Distribution.
    before_run:
      - _test
    steps:
      - script:
          title: flutter pub get + iOS config prep
          inputs:
            - content: |-
                #!/usr/bin/env bash
                set -ex
                flutter pub get
                dart pub global activate flutterfire_cli
                envman add --key PATH --value "$HOME/.pub-cache/bin:$PATH"
                flutter build ios --config-only --release
      - manage-ios-code-signing@2:
          inputs:
            - distribution_method: ad-hoc
            - apple_service_connection: api-key
            - project_path: "./ios/Runner.xcworkspace"
            - scheme: Runner
      - xcode-archive@5:
          inputs:
            - project_path: "./ios/Runner.xcworkspace"
            - scheme: Runner
            - configuration: Release
            - distribution_method: ad-hoc
            - automatic_code_signing: api-key
      - firebase-app-distribution@0:
          title: Firebase Deployment - Release IPA
          inputs:
            - app_path: "$BITRISE_IPA_PATH"
            - app: "$FIREBASE_IOS_APP_ID"
            - groups: "$TEST_GROUPS"
            - release_notes: "$BITRISE_GIT_MESSAGE"
            - firebase_token: "$FIREBASE_TOKEN"
      - deploy-to-bitrise-io@2: {}

  run_tests:
    description: Runs flutter analyze + tests on push to develop and all PRs.
    steps:
      - git-clone@8: {}
      - flutter-installer@1:
          inputs:
            - version: "$FLUTTER_VERSION"
      - restore-dart-cache@3: {}
      - flutter-analyze@0:
          inputs:
            - project_location: "$BITRISE_FLUTTER_PROJECT_LOCATION"
      - flutter-test@1:
          inputs:
            - project_location: "$BITRISE_FLUTTER_PROJECT_LOCATION"
      - save-dart-cache@1: {}
      - deploy-to-bitrise-io@2: {}
    triggers:
      push:
        - branch: develop
      pull_request:
        - source_branch: "*"

meta:
  bitrise.io:
    stack: osx-xcode-26.6.x
    machine_type_id: g2.mac.large

app:
  envs:
    - opts:
        is_expand: false
      BITRISE_FLUTTER_PROJECT_LOCATION: "."
    - opts:
        is_expand: false
      FLUTTER_VERSION: 3.44.8
    - opts:
        is_expand: false
      FIREBASE_ANDROID_APP_ID: "1:645600819281:android:e618ecc14b4118c849a070"
    - opts:
        is_expand: false
      FIREBASE_IOS_APP_ID: "1:645600819281:ios:c1c6acb94446b90249a070"
    - opts:
        is_expand: false
      TEST_GROUPS: "Solo"
    - opts:
        is_expand: false
      _JAVA_OPTIONS: "-Xms1024m -Xmx3072m"
    - opts:
        is_expand: false
      GRADLE_OPTS: "-Dorg.gradle.jvmargs=\"-Xmx3072m -XX:+HeapDumpOnOutOfMemoryError\""
