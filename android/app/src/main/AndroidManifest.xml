<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>
    <application
        android:label="Hopscotch"
        android:name=".HopscotchApplication"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Custom scheme deep links: hopscotch://<route>?id=<value> —
                 from push notifications (CleverTap wzrk_dl), API action
                 URLs, and in-app WebView redirects. No domain verification
                 needed; the OS always routes this scheme to us. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="hopscotch"/>
            </intent-filter>

            <!-- Android App Links: https://www.hopscotch.in/... opens the
                 app directly instead of a browser. autoVerify requires
                 https://www.hopscotch.in/.well-known/assetlinks.json to be
                 hosted and list this app's package + signing cert SHA-256
                 fingerprint(s) — see PR notes; not something this manifest
                 change alone can satisfy. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="www.hopscotch.in"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <meta-data
            android:name="CLEVERTAP_ACCOUNT_ID"
            android:value="${CLEVERTAP_ACCOUNT_ID}" />
        <meta-data
            android:name="CLEVERTAP_TOKEN"
            android:value="${CLEVERTAP_TOKEN}" />
        <meta-data
            android:name="CLEVERTAP_USE_GOOGLE_AD_ID"
            android:value="1" />
        <meta-data
            android:name="CLEVERTAP_BACKGROUND_SYNC"
            android:value="1" />
        <!-- Points at the app launcher icon as a placeholder small icon for
             the status bar. Swap for a proper white/alpha-only monochrome
             drawable (CleverTap docs: "Notification Icon") for a crisper
             status-bar rendering. -->
        <meta-data
            android:name="CLEVERTAP_NOTIFICATION_ICON"
            android:value="@mipmap/ic_launcher" />
        <!-- Firebase project_number (android/app/google-services.json),
             "id:" prefix required by CleverTap. -->
        <meta-data
            android:name="FCM_SENDER_ID"
            android:value="id:645600819281" />

        <!-- CleverTap push templates: renders rating / five-icon / timer /
             carousel / zero-bezel / input-box notifications and handles their
             action buttons. -->
        <service
            android:name="com.clevertap.android.sdk.pushnotification.CTNotificationIntentService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.clevertap.PUSH_EVENT" />
            </intent-filter>
        </service>
        <receiver
            android:name="com.clevertap.android.sdk.pushnotification.CTPushNotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Forwards every FCM message to CleverTap for rendering, alongside
             Flutter's own firebase_messaging handling. -->
        <service
            android:name=".notification.FCMMessageListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
